## Autocorrelação em Séries Temporais Estacionárias ARMA

### Introdução
A autocorrelação é uma ferramenta fundamental na análise de séries temporais, permitindo identificar padrões e dependências temporais nos dados. Este capítulo aprofunda o conceito de **autocorrelação** ($\rho_j$) em séries temporais estacionárias ARMA, expandindo a definição e propriedades apresentadas anteriormente [^3]. A autocorrelação quantifica a relação linear entre valores da série temporal em diferentes pontos no tempo, fornecendo *insights* valiosos sobre a estrutura temporal dos dados.

### Conceitos Fundamentais
A autocorrelação ($\rho_j$) de uma série temporal estacionária ARMA é definida como a correlação entre $$Y_t$$ e $$Y_{t-j}$$, onde *j* representa o *lag* ou o número de períodos de tempo que separam as duas observações [^49, 3.3.6]. Matematicamente, a autocorrelação é expressa como:

$$\rho_j = \frac{Cov(Y_t, Y_{t-j})}{\sqrt{Var(Y_t)Var(Y_{t-j})}}$$

Como a série é estacionária, a variância é constante ao longo do tempo, ou seja, $$Var(Y_t) = Var(Y_{t-j}) = \gamma_0$$. Além disso, $$Cov(Y_t, Y_{t-j})$$ é a autocovariância $$\gamma_j$$. Portanto, a equação simplifica para:

$$\rho_j = \frac{\gamma_j}{\gamma_0}$$

A autocorrelação é uma medida adimensional, variando entre -1 e 1 [^49]. Um valor de $$\rho_j$$ próximo de 1 indica uma forte correlação positiva entre $$Y_t$$ e $$Y_{t-j}$$, o que significa que valores altos (ou baixos) em *t* tendem a ser seguidos por valores altos (ou baixos) em *t-j*. Um valor de $$\rho_j$$ próximo de -1 indica uma forte correlação negativa, onde valores altos em *t* tendem a ser seguidos por valores baixos em *t-j*, e vice-versa. Um valor de $$\rho_j$$ próximo de 0 sugere uma correlação linear fraca ou inexistente.

> 💡 **Exemplo Numérico:**
>
> Imagine uma série temporal representando as vendas mensais de uma loja. Se a autocorrelação no *lag* 1 ($\rho_1$) for 0.8, isso sugere que as vendas em um determinado mês estão fortemente correlacionadas com as vendas do mês anterior. Um valor alto de vendas em um mês tende a ser seguido por um valor alto no mês seguinte. Por outro lado, se $\rho_1$ fosse -0.7, um mês com vendas altas tenderia a ser seguido por um mês com vendas baixas. Se $\rho_1$ fosse próximo de 0 (por exemplo, 0.1), as vendas de um mês seriam praticamente independentes das vendas do mês anterior.

É importante notar que $$\rho_0 = \frac{\gamma_0}{\gamma_0} = 1$$ [^49], o que significa que a correlação de uma série consigo mesma (sem *lag*) é sempre igual a 1. Isso serve como um ponto de referência na análise da função de autocorrelação (ACF).

**Prova:**
A prova de que $$\rho_0 = 1$$ é direta:

I. Pela definição, $$\rho_0 = \frac{Cov(Y_t, Y_{t-0})}{\sqrt{Var(Y_t)Var(Y_{t-0})}}$$
II. Como $$Y_{t-0} = Y_t$$, temos $$\rho_0 = \frac{Cov(Y_t, Y_t)}{\sqrt{Var(Y_t)Var(Y_t)}}$$
III. Sabemos que $$Cov(Y_t, Y_t) = Var(Y_t)$$, logo $$\rho_0 = \frac{Var(Y_t)}{Var(Y_t)}$$
IV. Portanto, $$\rho_0 = 1$$ ■

**Função de Autocorrelação (ACF)**: A ACF é um gráfico que plota os valores de $$\rho_j$$ em função de *j* (o *lag*). A ACF fornece uma representação visual da dependência temporal na série temporal, revelando quais *lags* são significativamente correlacionados. A análise da ACF é crucial para identificar a ordem de modelos ARMA apropriados para a série temporal [^7].

Para complementar a análise da ACF, é útil considerar também os intervalos de confiança para as autocorrelações. Isso permite determinar se os valores de $$\rho_j$$ são estatisticamente significativos, ou seja, se são diferentes de zero em um nível de significância predeterminado.

**Intervalos de Confiança para ACF**:
Sob a hipótese nula de que a série temporal é puramente aleatória (ruído branco), os valores de $$\rho_j$$ são aproximadamente normalmente distribuídos com média zero e variância $$\frac{1}{T}$$, onde *T* é o tamanho da amostra [^60]. Portanto, um intervalo de confiança aproximado de 95% para $$\rho_j$$ é dado por:

$$IC_{95\%}(\rho_j) = \pm \frac{1.96}{\sqrt{T}}$$

Valores de $$\rho_j$$ que caem fora deste intervalo são considerados estatisticamente significativos e indicam uma dependência temporal não aleatória na série.

> 💡 **Exemplo Numérico:**
>
> Suponha que temos uma série temporal com *T* = 100 observações. O intervalo de confiança de 95% para a ACF seria:
>
> $$IC_{95\%}(\rho_j) = \pm \frac{1.96}{\sqrt{100}} = \pm 0.196$$
>
> Isso significa que, se a série fosse ruído branco, esperaríamos que 95% das autocorrelações amostrais estivessem entre -0.196 e 0.196. Se observarmos um valor de $$\rho_3 = 0.3$$, este valor estaria fora do intervalo de confiança e, portanto, seria considerado estatisticamente significativo, sugerindo que há uma dependência temporal no *lag* 3.
>
> ```python
> import numpy as np
>
> T = 100
> ic_95 = 1.96 / np.sqrt(T)
> print(f"Intervalo de Confiança de 95%: +/- {ic_95:.3f}")
> ```

**Lema 1**: *Para uma série temporal de ruído branco com tamanho amostral T, a probabilidade de encontrar pelo menos um lag j com autocorrelação significativamente diferente de zero (fora do intervalo de confiança de 95%) aumenta com o aumento do número de lags considerados.*

*Prova:*
A probabilidade de que uma única autocorrelação estimada $$\rho_j$$ esteja dentro do intervalo de confiança de 95% é de 0.95. Se analisarmos *m* lags, a probabilidade de que todas as *m* autocorrelações estejam dentro dos respectivos intervalos de confiança é $$0.95^m$$. Portanto, a probabilidade de que pelo menos uma autocorrelação esteja fora do intervalo de confiança é $$1 - 0.95^m$$. À medida que *m* aumenta, $$1 - 0.95^m$$ também aumenta, demonstrando o lema.

> 💡 **Exemplo Numérico:**
>
> Vamos calcular a probabilidade de encontrar pelo menos uma autocorrelação significativa em uma série de ruído branco para diferentes números de lags (*m*):
>
> *   Para *m* = 1 lag: $$1 - 0.95^1 = 0.05$$ (5% de chance)
> *   Para *m* = 5 lags: $$1 - 0.95^5 \approx 0.226$$ (22.6% de chance)
> *   Para *m* = 10 lags: $$1 - 0.95^{10} \approx 0.401$$ (40.1% de chance)
> *   Para *m* = 20 lags: $$1 - 0.95^{20} \approx 0.642$$ (64.2% de chance)
>
> Este exemplo ilustra como a probabilidade de identificar um falso positivo aumenta consideravelmente com o número de lags analisados.
>
> ```python
> import numpy as np
>
> lags = [1, 5, 10, 20]
> probabilities = [1 - (0.95)**m for m in lags]
>
> for m, prob in zip(lags, probabilities):
>     print(f"Probabilidade para m={m} lags: {prob:.3f}")
> ```

Esse lema reforça a importância de cautela na interpretação da ACF, especialmente quando se analisam muitos lags, pois a chance de identificar falsos positivos aumenta.

**Propriedades da Autocorrelação**:

1.  **Simetria**: Para processos estacionários, a autocorrelação é simétrica, ou seja, $$\rho_j = \rho_{-j}$$. Isso significa que a correlação entre $$Y_t$$ e $$Y_{t-j}$$ é a mesma que a correlação entre $$Y_t$$ e $$Y_{t+j}$$.

**Prova:**
Para provar a simetria da autocorrelação, mostramos que  $$\rho_j = \rho_{-j}$$:
I.  Por definição, $$\rho_j = \frac{\gamma_j}{\gamma_0} = \frac{Cov(Y_t, Y_{t-j})}{Var(Y_t)}$$
II.  Similarmente, $$\rho_{-j} = \frac{\gamma_{-j}}{\gamma_0} = \frac{Cov(Y_t, Y_{t+j})}{Var(Y_t)}$$
III. Como a série é estacionária, a autocovariância depende apenas da distância entre os pontos no tempo, então $$Cov(Y_t, Y_{t-j}) = Cov(Y_{t+j}, Y_t)$$
IV. Usando a propriedade de simetria da covariância, $$Cov(Y_{t+j}, Y_t) = Cov(Y_t, Y_{t+j})$$
V. Portanto, $$\rho_{-j} = \frac{Cov(Y_t, Y_{t+j})}{Var(Y_t)} = \frac{Cov(Y_t, Y_{t-j})}{Var(Y_t)} = \rho_j$$ ■

2.  **Decaimento**: A autocorrelação de um processo estacionário tende a decair à medida que *j* aumenta. A velocidade e o padrão desse decaimento fornecem informações importantes sobre a memória do processo. Por exemplo, processos AR têm um decaimento exponencial ou amortecido, enquanto processos MA têm um corte abrupto após um certo *lag* [^53, 56].

> 💡 **Exemplo Numérico:**
>
> Considere um processo AR(1) com $$\phi = 0.5$$. As autocorrelações para os primeiros lags seriam:
>
> *   $$\rho_0 = 1$$
> *   $$\rho_1 = 0.5^1 = 0.5$$
> *   $$\rho_2 = 0.5^2 = 0.25$$
> *   $$\rho_3 = 0.5^3 = 0.125$$
> *   $$\rho_4 = 0.5^4 = 0.0625$$
>
> Observe como a autocorrelação decai exponencialmente. Após alguns lags, a autocorrelação se torna muito pequena, indicando que a dependência temporal diminui com o aumento da distância no tempo. Já em um processo MA(1), a autocorrelação é zero para lags maiores que 1, indicando uma memória curta.
>
> ```python
> import numpy as np
> import matplotlib.pyplot as plt
>
> phi = 0.5
> lags = np.arange(5)
> autocorrelations = phi ** lags
>
> plt.stem(lags, autocorrelations)
> plt.title('Autocorrelação do Processo AR(1) com phi=0.5')
> plt.xlabel('Lag (j)')
> plt.ylabel('Autocorrelação (rho_j)')
> plt.show()
> ```

Para auxiliar na identificação do decaimento, podemos definir uma medida de "meia-vida" da autocorrelação.

**Definição**: A meia-vida da autocorrelação é o número de lags (*j*) necessários para que a magnitude da autocorrelação ($$|\rho_j|$$) caia abaixo da metade de seu valor inicial ($$|\rho_1|$$).

Essa medida pode ajudar a comparar a persistência da autocorrelação entre diferentes séries temporais ou modelos.

> 💡 **Exemplo Numérico:**
>
> Para o exemplo anterior do processo AR(1) com $$\phi = 0.5$$, temos $$\rho_1 = 0.5$$. Queremos encontrar o menor *j* tal que $$|\rho_j| < \frac{|\rho_1|}{2} = \frac{0.5}{2} = 0.25$$.
>
> *   $$\rho_1 = 0.5$$
> *   $$\rho_2 = 0.25$$
> *   $$\rho_3 = 0.125$$
>
> Neste caso, a meia-vida é 3, pois $$\rho_3 = 0.125 < 0.25$$. Isso significa que a autocorrelação "perde" metade de sua força em 3 lags.

3.  **Relação com Autocovariância**: Como mencionado anteriormente, a autocorrelação é uma versão normalizada da autocovariância. Portanto, as propriedades da autocovariância também influenciam a autocorrelação.

**Teorema 1**: *Se a autocovariância $$\gamma_j$$ de uma série temporal estacionária é absolutamente somável, ou seja, $$\sum_{j=-\infty}^{\infty} |\gamma_j| < \infty$$, então a série temporal é dita ter memória curta.*

*Prova:*
A condição $$\sum_{j=-\infty}^{\infty} |\gamma_j| < \infty$$ implica que o impacto das observações passadas decai rapidamente, de modo que a dependência entre observações distantes no tempo torna-se negligível. Em outras palavras, a série temporal "esquece" seu passado rapidamente, caracterizando um comportamento de memória curta.

É importante notar que a condição de absolutamente somável para a autocovariância é uma condição suficiente, mas não necessária, para a memória curta.

**Exemplo de Autocorrelação em um Processo MA(1)**:
Considere o processo MA(1):

$$Y_t = \mu + \varepsilon_t + \theta \varepsilon_{t-1}$$

A autocorrelação no *lag* 1 ($\rho_1$) para este processo é dada por [^49, 3.3.7]:

$$\rho_1 = \frac{\theta}{1 + \theta^2}$$

E as demais autocorrelações são zero ($\rho_j = 0$ para *j > 1*) [^49]. Isso demonstra que o processo MA(1) tem memória apenas no *lag* 1.

> 💡 **Exemplo Numérico:**
>
> Se $$\theta = 0.5$$ no processo MA(1), então:
>
> $$\rho_1 = \frac{0.5}{1 + 0.5^2} = \frac{0.5}{1.25} = 0.4$$
>
> Isso significa que a correlação entre $$Y_t$$ e $$Y_{t-1}$$ é 0.4. Para todos os outros lags (*j* > 1), $$\rho_j = 0$$.
>
> Se $$\theta = -0.8$$ no processo MA(1), então:
>
> $$\rho_1 = \frac{-0.8}{1 + (-0.8)^2} = \frac{-0.8}{1.64} \approx -0.488$$
>
> Neste caso, a correlação entre $$Y_t$$ e $$Y_{t-1}$$ é aproximadamente -0.488.

**Lema 2**: *Para o processo MA(1), a autocovariância $$\gamma_j$$ é absolutamente somável.*

*Prova:*
Para o processo MA(1), temos que $$\gamma_0 = (1+\theta^2)\sigma^2$$ , $$\gamma_1 = \theta \sigma^2$$ e $$\gamma_j = 0$$ para $$j > 1$$.
Portanto, $$\sum_{j=-\infty}^{\infty} |\gamma_j| = |\gamma_{-1}| + |\gamma_0| + |\gamma_1| = |\theta \sigma^2| + |(1+\theta^2)\sigma^2| + |\theta \sigma^2| = 2|\theta|\sigma^2 + (1+\theta^2)\sigma^2 < \infty$$, uma vez que $$\theta$$ e $$\sigma^2$$ são parâmetros finitos. Assim, a autocovariância é absolutamente somável.

Como consequência do Teorema 1 e do Lema 2, podemos afirmar que o processo MA(1) é um processo de memória curta.

**Exemplo de Autocorrelação em um Processo AR(1)**:
Agora, considere o processo AR(1):

$$Y_t = \phi Y_{t-1} + \varepsilon_t$$

A autocorrelação no *lag* j ($\rho_j$) para este processo é dada por:

$$\rho_j = \phi^j$$

Este resultado mostra que a autocorrelação em um processo AR(1) decai exponencialmente com o aumento do lag *j*. A taxa de decaimento é determinada pelo coeficiente $$\phi$$. Se $$|\phi| < 1$$, a autocorrelação converge para zero à medida que *j* aumenta.

> 💡 **Exemplo Numérico:**
>
> Considere um processo AR(1) com $$\phi = 0.8$$. As autocorrelações para alguns lags seriam:
>
> *   $$\rho_0 = 1$$
> *   $$\rho_1 = 0.8^1 = 0.8$$
> *   $$\rho_2 = 0.8^2 = 0.64$$
> *   $$\rho_3 = 0.8^3 = 0.512$$
> *   $$\rho_4 = 0.8^4 = 0.4096$$
>
> Se $$\phi = -0.6$$, as autocorrelações seriam:
>
> *   $$\rho_0 = 1$$
> *   $$\rho_1 = (-0.6)^1 = -0.6$$
> *   $$\rho_2 = (-0.6)^2 = 0.36$$
> *   $$\rho_3 = (-0.6)^3 = -0.216$$
> *   $$\rho_4 = (-0.6)^4 = 0.1296$$
>
> Neste caso, a autocorrelação alterna entre valores positivos e negativos, mas ainda decai exponencialmente em magnitude.

**Teorema 2**: *O processo AR(1) é estacionário se e somente se $$|\phi| < 1$$.*

Este teorema é fundamental para garantir que as propriedades estatísticas do processo AR(1) sejam bem definidas e que a autocorrelação decaia para zero à medida que o lag aumenta.

**Prova:**
A prova da estacionariedade do AR(1) quando $$|\phi|<1$$ pode ser feita da seguinte forma:

I. Podemos reescrever o processo AR(1) recursivamente:
   $$Y_t = \phi Y_{t-1} + \varepsilon_t = \phi (\phi Y_{t-2} + \varepsilon_{t-1}) + \varepsilon_t = \phi^2 Y_{t-2} + \phi \varepsilon_{t-1} + \varepsilon_t$$
II. Continuando a substituição, obtemos:
   $$Y_t = \sum_{i=0}^{\infty} \phi^i \varepsilon_{t-i}$$
III. Para que $$Y_t$$ seja estacionário, sua média e variância devem ser constantes ao longo do tempo e finitas.
IV. A média de $$Y_t$$ é:
    $$E[Y_t] = E\left[\sum_{i=0}^{\infty} \phi^i \varepsilon_{t-i}\right] = \sum_{i=0}^{\infty} \phi^i E[\varepsilon_{t-i}] = 0$$  (assumindo que $$E[\varepsilon_t] = 0$$)
V. A variância de $$Y_t$$ é:
   $$Var(Y_t) = Var\left(\sum_{i=0}^{\infty} \phi^i \varepsilon_{t-i}\right) = \sum_{i=0}^{\infty} \phi^{2i} Var(\varepsilon_{t-i}) = \sigma^2 \sum_{i=0}^{\infty} \phi^{2i}$$  (assumindo que $$Var(\varepsilon_t) = \sigma^2$$)
VI. A soma $$\sum_{i=0}^{\infty} \phi^{2i}$$ é uma série geométrica que converge se $$|\phi^2| < 1$$, ou seja, $$|\phi| < 1$$. Neste caso, a soma é igual a $$\frac{1}{1 - \phi^2}$$.
VII. Portanto, se $$|\phi| < 1$$, a variância de $$Y_t$$ é:
    $$Var(Y_t) = \frac{\sigma^2}{1 - \phi^2}$$
VIII. Como a média e a variância de $$Y_t$$ são constantes e finitas quando $$|\phi| < 1$$, o processo AR(1) é estacionário sob esta condição. A prova da não estacionariedade quando $$|\phi| \geq 1$$ é mais complexa e envolve mostrar que a variância diverge. ■

**Teorema 2.1**: *Se o processo AR(1) é estacionário ($$|\phi| < 1$$), então a autocovariância $$\gamma_j$$ é absolutamente somável, e portanto o processo AR(1) é de memória curta.*

*Prova:*
Se $$|\phi| < 1$$, então $$\gamma_j = \phi^j \gamma_0$$.  Portanto,
$$\sum_{j=-\infty}^{\infty} |\gamma_j| = \sum_{j=-\infty}^{\infty} |\phi^j \gamma_0| = |\gamma_0| \sum_{j=-\infty}^{\infty} |\phi^j| = |\gamma_0| \left( \sum_{j=-\infty}^{-1} |\phi^j| + 1 + \sum_{j=1}^{\infty} |\phi^j| \right)$$
Como $$|\phi| < 1$$, as somas geométricas convergem:
$$\sum_{j=1}^{\infty} |\phi^j| = \frac{|\phi|}{1-|\phi|}$$ e $$\sum_{j=-\infty}^{-1} |\phi^j| = \sum_{k=1}^{\infty} |\phi^{-k}| = \sum_{k=1}^{\infty} \left(\frac{1}{|\phi|}\right)^k = \frac{\frac{1}{|\phi|}}{1 - \frac{1}{|\phi|}} = \frac{1}{ |\phi| - 1 } = \frac{-1}{1-|\phi|}$$
Assim, $$\sum_{j=-\infty}^{\infty} |\gamma_j| = |\gamma_0| \left( \frac{1}{1-|\phi|} + 1 + \frac{|\phi|}{1-|\phi|} \right) = |\gamma_0| \left( \frac{2-|\phi|}{1-|\phi|} \right) < \infty$$.

Portanto, se o processo AR(1) é estacionário, sua autocovariância é absolutamente somável, implicando que ele é de memória curta.

### Conclusão
A autocorrelação ($\rho_j$) é uma ferramenta essencial na análise de séries temporais estacionárias ARMA, permitindo quantificar e visualizar a dependência temporal nos dados. Ao analisar a função de autocorrelação (ACF), é possível identificar padrões, memória e a ordem de modelos ARMA adequados para representar a série temporal. O 0th autocorrelação sendo sempre igual a 1 fornece um importante ponto de referência. Compreender a autocorrelação é fundamental para modelagem, previsão e inferência estatística em séries temporais [^7, 49]. A consideração de intervalos de confiança e medidas como a meia-vida da autocorrelação auxiliam em uma análise mais robusta e informativa.

### Referências
[^3]: Informação retirada da introdução do capítulo.
[^49]: Informação retirada da página 49, seção 3.3.6.
[^7]: Informação retirada da página 47.
[^53]: Informação retirada da página 53.
[^56]: Informação retirada da página 56.
[^60]: Estatística para Economia e Administração, Morettin e Bussab, 7ª Edição.
<!-- END -->